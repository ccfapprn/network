

- if !users.any?
  .alert.alert-warning No People Fit This Role Yet.
- else
  .well
    .row
      - users.each do |user|
        .col-sm-2
          = image_tag user.community_photo_url, :class => "img-circle" if user.community_photo_url
          .small
            %strong
              = user.community_name

%br/
