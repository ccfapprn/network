
%h2 Team

.panel.panel-default
  -# .panel-heading
  -#   %h3.panel-title Governance Team


  .panel-body

    - [["Patient Team",User.with_role(:patient_team)],["Research Team", User.with_role(:research_team)], ["Tech Team",User.with_role(:patient_team)]].each do |title,team|

      %h4= title


      - team.each do |user|
        .row
          - 3.times do
            .col-sm-2
              = image_tag user.community_photo_url, :class => "img-circle" if user.community_photo_url
              .small
                %strong
                  = "Ms. Lorem Ipsum" || user.community_name
        %hr/