-# Bio Spot

- if current_user
  .panel.panel-primary
    .panel-body
      .media
        .pull-left
          = image_tag current_user.private_photo_url, :class => " media-item-object img-circle user-avatar"
        .media-body.small
          %h4
            %a{href: social_profile_path}
              = current_user.private_name
            %br/
            %div{"style" => "margin-top: 5px;"}
              -#, "data-toggle" => "tooltip", "data-placement" => "right", "title" =>"Citizen Scientist Level: The total number of badges you've earned."}
              %span.label.label-as-badge.label-default.label-md
                %i.fa.fa-flask
                = current_user.badges.count

    .panel-footer.small.center
      - if current_user.social_profile.visible_to_community?
        Social is On: This bio is shared with others.
      - else
        Social is Off: You are hidden to other users.
      %a{href: social_profile_path}
        Change this.


