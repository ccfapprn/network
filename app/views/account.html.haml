-##############
-### ACCOUNT SETTINGS
-##############

-##############
-### YOUR CONTRIBUTIONS
-##############
- content_for :your_contributions do
  = render partial: "shared/badge_list" if current_user



- content_for :research_content do
  %ul.nav.nav-tabs{role: "tablist"}
    %li{class: "#{'active' if @active_sub_nav == :my_trends}"}
      %a{href: my_trends_path} My Social Profile
    %li{class: "#{'active' if @active_sub_nav == :my_health_measures}"}
      %a{href: my_health_measures_path} My User Details
  = yield


= render template: "layouts/community"
