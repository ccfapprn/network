
.panel.panel-default
  .panel-heading
    %h3.panel-title Data Access Logs


  - if @research_access_events.empty?
    .panel-body
      .alert.alert-info Your data has not been accessed yet.
  - else
    %table.table
      %thead
        %th Date
        %th Description
      %tbody
        - @research_access_events.each do |event|
          - time = Time.parse(event['timestamp'])
          %tr
            %td= my_strftime(time) if time
            %td
              = event['description']
              = link_to " Learn More.", event['url'] if event['url']
